# Normalize line endings: store as LF in the repo, check out native on Windows.
* text=auto eol=lf

# Treat these as binary (never munge line endings).
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.ico binary
*.woff binary
*.woff2 binary
